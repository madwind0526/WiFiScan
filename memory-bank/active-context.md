# Active Context

## Current Focus

- Wave 26까지 완료, 앱 버전 `1.4.0+10`. analyze 무이슈 / 95 tests 통과 / Windows·SM S948N 실기기 확인 / push 완료. 세부 내역은 `STATE.md`, 재사용 지식은 `knowledge/`에 있다.
- 사용자 망은 라우터 3대다: `192.168.0.1` ipTIME(캡차 없음, 자동 로그인), `192.168.45.1` SK GW-ME6110(캡차 팝업), `192.168.75.1` GNT2400 IPTV(ipTIME의 WAN 상위). 레지스트리 실측: `iptime` / `sk-broadband` / `NO MATCH`.
- **다음 후보 (미착수)**
  - PC↔폰 프로필 전달을 실제로 한 번 돌려보기. PC에는 Windows에서 가져온 암호 4개가 있고 폰에는 없다. 암호화 내보내기/가져오기는 이미 구현돼 있으나 기기 간 실사용 검증은 안 했다.
  - GNT2400(192.168.75.1) 커넥터: **사용자도 관리자 계정을 몰라 보류**. 실익도 낮다(경유 라우터, 장비 21개는 45.1에서 이미 보임). 자격 증명 없이 다시 파헤치지 말 것.
  - 최신 ipTIME 펌웨어의 캡차 로그인 지원(현재 커넥터는 캡차 없는 경로만 사용).
- Android 17 target SDK 37 이상에서 로컬 LAN 접근 권한 대응이 필요하다(기존 과제, 유효).
